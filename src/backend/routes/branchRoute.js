const express = require('express');
const router = express.Router();

router.get('/',);
router.get('/:branchId');
router.post('/',);
router.put('/:branchId',);
router.delete('/',)

export default router;